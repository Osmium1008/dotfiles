---@type LazySpec
---@diagnostic disable-next-line: missing-fields
return {
    "williamboman/mason.nvim",
    lazy = false,
    ---@type MasonSettings
    opts = {
        ui = {
            border = "single",
        },
    },
}
